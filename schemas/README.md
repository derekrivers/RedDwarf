# Schemas

Runtime JSON contracts are authored in TypeScript with Zod under `packages/contracts`.

This directory is reserved for exported snapshots or generated artifacts that OpenClaw may need to mount independently of the source packages.
