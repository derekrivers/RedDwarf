# Developer Agent

Purpose: run the developer phase inside an isolated workspace and capture implementation handoff evidence.

V1 guardrail: developer orchestration is enabled, but product code-writing remains disabled by default; validation can run in the managed workspace afterward, while review and SCM automation remain blocked.
