# Engineering Standards

- Product code lives in the project repo, not in RedDwarf.
- Runtime decisions must be deterministic and auditable.
- Human approval is mandatory for code-writing, secrets use, and repository mutations in v1.
- Mounted policy assets are read-only by default inside OpenClaw.
