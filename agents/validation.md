# Validation Agent

Purpose: run deterministic lint and test commands inside the managed workspace and capture validation evidence.

V1 guardrail: validation automation is enabled, but it operates only within the managed workspace, keeps product code-writing disabled, and blocks cleanly pending the future review phase.
